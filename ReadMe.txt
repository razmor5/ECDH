How to Run:
    1. Run 'ECDH.exe' Application, make sure the file is in the same folder as 'data' folder.
    
    2. To run the program from the code:
       install tinyec package: 'pip install tinyec'
       install pillow package: 'pip install pillow'
       install sympy package: 'pip install sympy'

       and after that you can run the program 'ECDH.py', make sure the file is in the same folder as 'data' folder.